<%= link_to "New Tour", new_tour_path %>
<% @tours.each do |tour| %>
  <h3><%= tour.tour_name.html_safe %></h3>
  <p><%= tour.description.html_safe %></p>
  <%= link_to "Edit Tour", edit_tour_path(tour) %>
<% end %>
